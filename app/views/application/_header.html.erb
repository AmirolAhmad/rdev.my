<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <%= link_to "rdev.my", root_path, class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup"
    aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <a class="nav-item nav-link" href="#">Features</a>
      <a class="nav-item nav-link" href="#">Pricing</a>
      <% if user_signed_in? %>
        <%= link_to "Sign out", destroy_user_session_path, method: :delete, class: "nav-item nav-link", data: { turbolinks: false } %>
      <% else %>
        <%= link_to login_path, class: "nav-item nav-link", data: { turbolinks: false } do %>
          <i class="fas fa-user-lock"></i> Sign In
        <% end %> 
        <%= link_to "Sign up", register_path, class: "nav-item nav-link", data: { turbolinks: false } %>
      <% end %>
    </div>
  </div>
</nav>